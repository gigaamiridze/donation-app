import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  image: {
    width: '100%',
    height: 240,
    marginTop: 12,
    marginBottom: 24,
    borderRadius: 5,
  },
  title: {
    marginTop: 16,
    marginBottom: 7,
  },
});
